plugin: aws_ec2
cache: True 

regions:
  - us-east-2

filters:
  tag:demo: Ansible

hostnames:
  - dns-name

compose: 
  private_dns_name: private-dns-name 
  ec2_id: instance_id
  
keyed_groups:
  - key: 'tags.group_name'
    prefix: ""
    separator: ""